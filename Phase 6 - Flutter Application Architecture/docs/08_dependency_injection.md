# Dependency Injection

Dependency injection supplies a class with the collaborators it needs instead of making it construct them itself.

Learn dependency inversion, constructor injection, registration, and provider-based injection.

