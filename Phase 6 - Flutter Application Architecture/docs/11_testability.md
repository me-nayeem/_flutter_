# Testability

Good architecture makes UI, state holders, business rules, repositories, and services independently testable.

Use clear boundaries and injected dependencies to make testing practical.

